# Optional local ML packages — required for the analysis pipeline, not for unit tests.
# Install after `git submodule update --init --recursive`.
-e ../silero-vad
-e ../whisperX
auto-editor>=23.44.1
google-generativeai>=0.8.6
